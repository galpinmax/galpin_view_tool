require "spec_helper"

describe GalpinViewTool do
  it "has a version number" do
    expect(GalpinViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
